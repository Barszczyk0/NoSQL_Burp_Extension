public enum PayloadType {
    FUZZ_STRING,
    BOOLEAN,
    AUTHENTICATION_BYPASS_USERNAME,     // Payloads for username fields
    AUTHENTICATION_BYPASS_PASSWORD,     // Payloads for passwords fields
    DATA_EXTRACTION,
    TIME_BASED
}
